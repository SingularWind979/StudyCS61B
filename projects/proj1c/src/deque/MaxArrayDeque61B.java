package deque;

import java.util.Comparator;

/**
 * A deque that can find the maximum element in the deque.
 *
 * @param <T> The type of elements in the deque.
 * @author SingularWind
 * @version 1.0
 */
public class MaxArrayDeque61B<T> extends ArrayDeque61B<T> {
    /**
     * Constructs a new MaxArrayDeque61B with the given comparator.
     *
     * @param comparator The comparator to use for comparing elements.
     */
    public MaxArrayDeque61B(Comparator<T> comparator) {
    }

    /**
     * Finds the maximum element in the deque.
     *
     * @return The maximum element in the deque.
     */
    public boolean[] max() {
        return null;
    }
}
